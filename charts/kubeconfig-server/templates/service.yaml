apiVersion: v1
kind: Service
metadata:
  name: {{ include "kubeconfig-server.fullname" . }}
  labels:
    {{- include "kubeconfig-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "kubeconfig-server.selectorLabels" . | nindent 4 }}
