---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: kubernetesclusters.cloud.nicklasfrahm.dev
spec:
  group: cloud.nicklasfrahm.dev
  names:
    kind: KubernetesCluster
    listKind: KubernetesClusterList
    plural: kubernetesclusters
    singular: kubernetescluster
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: KubernetesCluster is the Schema for the kubernetesclusters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KubernetesClusterSpec defines the desired state of KubernetesCluster
            properties:
              cluster:
                description: Cluster defines cluster-wide configuration.
                properties:
                  allowSchedulingOnControlPlanes:
                    default: false
                    description: AllowSchedulingOnControlPlanes allows pods to be
                      scheduled on control plane nodes.
                    type: boolean
                type: object
              infrastructure:
                description: Infrastructure defines infrastructure-specific configuration.
                properties:
                  controlPlane:
                    description: ControlPlane defines control plane-specific configuration.
                    properties:
                      machinePools:
                        description: MachinePools defines the machine pools for the
                          control plane.
                        items:
                          description: KubernetesClusterSpecMachinePools defines machine
                            pool-specific configuration.
                          properties:
                            name:
                              description: Name is the name of the machine pool.
                              maxLength: 63
                              minLength: 1
                              type: string
                          required:
                          - name
                          type: object
                        minItems: 1
                        type: array
                    type: object
                required:
                - controlPlane
                type: object
            required:
            - infrastructure
            type: object
          status:
            description: KubernetesClusterStatus defines the observed state of KubernetesCluster
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
